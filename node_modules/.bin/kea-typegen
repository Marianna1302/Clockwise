#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../kea-typegen/dist/src/cli/typegen.js" "$@"
  ret=$?
else 
  node  "$basedir/../kea-typegen/dist/src/cli/typegen.js" "$@"
  ret=$?
fi
exit $ret
